server:
  port: 9527
spring:
  application:
    name: cloud-gateway
eureka:
  instance:
    hostname: localhost
  client: #服务提供者provider注册进eureka服务列表内
    service-url:
      register-with-eureka: true
      fetch-registry: true
      defaultZone: http://localhost:7001/eureka/